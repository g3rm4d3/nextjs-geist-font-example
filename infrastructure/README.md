# infrastructure

Reserved for deployment/infrastructure configuration (containers, IaC,
environment provisioning). Stage 1 does not deploy anywhere yet — this
directory is populated when a real deployment target is defined, per
`docs/deployment.md` in a later phase.
