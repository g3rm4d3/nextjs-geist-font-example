import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function RideDetailsScreen() {
  return (
    <PlaceholderScreen
      title="Ride Details"
      description="Per-ride detail (route, fare breakdown, driver, rating) is built once rides exist (Phase 7+)."
    />
  );
}
