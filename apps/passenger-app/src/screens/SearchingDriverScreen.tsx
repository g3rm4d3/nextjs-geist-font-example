import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function SearchingDriverScreen() {
  return (
    <PlaceholderScreen
      title="Searching for a Driver"
      description="The matching engine that finds and offers rides to nearby drivers is built in Phase 8."
    />
  );
}
