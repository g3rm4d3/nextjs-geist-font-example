import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function RideTrackingScreen() {
  return (
    <PlaceholderScreen
      title="Ride Tracking"
      description="Real-time driver location during a ride is built in Phase 10 (Realtime Ride Experience), on top of Phase 6's location infrastructure."
    />
  );
}
