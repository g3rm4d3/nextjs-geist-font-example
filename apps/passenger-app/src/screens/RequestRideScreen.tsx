import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function RequestRideScreen() {
  return (
    <PlaceholderScreen
      title="Request Ride"
      description="Ride requests and driver matching arrive in a later phase (Phase 7: Ride Request Service, Phase 8: Matching Engine). Your route is ready — there's just no one to send it to yet."
    />
  );
}
