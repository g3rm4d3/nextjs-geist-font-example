import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function RideCompleteScreen() {
  return (
    <PlaceholderScreen
      title="Ride Complete"
      description="The completed-ride summary (final fare, payment, rating prompt) is built alongside the ride lifecycle (Phase 9), payments (Phase 11), and ratings (Phase 13)."
    />
  );
}
