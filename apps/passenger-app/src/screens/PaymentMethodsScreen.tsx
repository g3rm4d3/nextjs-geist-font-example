import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function PaymentMethodsScreen() {
  return (
    <PlaceholderScreen
      title="Payment Methods"
      description="Stripe TEST MODE payment methods are added in Phase 11 (Payment Sandbox)."
    />
  );
}
