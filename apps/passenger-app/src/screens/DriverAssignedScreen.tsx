import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function DriverAssignedScreen() {
  return (
    <PlaceholderScreen
      title="Driver Assigned"
      description="Once matching (Phase 8) and the ride lifecycle (Phase 9) exist, this screen will show your driver, vehicle, and live ETA."
    />
  );
}
