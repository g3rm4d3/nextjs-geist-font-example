import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function EarningsScreen() {
  return (
    <PlaceholderScreen
      title="Earnings"
      description="There's nothing to show yet — driver_earnings rows only exist once rides complete (Phase 9) and the financial ledger (Phase 12) is built."
    />
  );
}
