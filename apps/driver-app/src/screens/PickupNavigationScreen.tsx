import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function PickupNavigationScreen() {
  return (
    <PlaceholderScreen
      title="Pickup Navigation"
      description="Turn-by-turn guidance to the passenger's pickup point is part of the realtime ride experience (Phase 10), once a ride and a driver are actually matched (Phase 8/9)."
    />
  );
}
