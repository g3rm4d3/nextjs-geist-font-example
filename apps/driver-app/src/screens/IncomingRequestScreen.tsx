import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function IncomingRequestScreen() {
  return (
    <PlaceholderScreen
      title="Incoming Request"
      description="Ride offers don't exist yet — matching/dispatch (Phase 8) is what will send a request here for a driver to accept or decline."
    />
  );
}
