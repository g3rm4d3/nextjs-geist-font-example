import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function HistoryScreen() {
  return (
    <PlaceholderScreen
      title="History"
      description="There's no ride data to show yet — rides don't exist until Phase 7 (Ride Request Service). This screen will list completed and cancelled rides once they do."
    />
  );
}
