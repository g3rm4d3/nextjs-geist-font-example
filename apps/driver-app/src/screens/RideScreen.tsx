import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function RideScreen() {
  return (
    <PlaceholderScreen
      title="Ride"
      description="The in-progress ride view (live route, passenger info, fare) is part of the realtime ride experience — Phase 9 (ride lifecycle) and Phase 10 (realtime)."
    />
  );
}
