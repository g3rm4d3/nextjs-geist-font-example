import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function RideCompleteScreen() {
  return (
    <PlaceholderScreen
      title="Ride Complete"
      description="A completed-ride summary (fare, earnings, rating prompt) needs rides (Phase 9), the financial ledger (Phase 12), and ratings (Phase 13) to all exist first."
    />
  );
}
