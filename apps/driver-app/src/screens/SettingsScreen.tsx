import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function SettingsScreen() {
  return (
    <PlaceholderScreen
      title="Settings"
      description="App preferences (notifications, units, etc.) are added alongside the phases that introduce something worth configuring."
    />
  );
}
