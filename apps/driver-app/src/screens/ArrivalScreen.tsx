import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function ArrivalScreen() {
  return (
    <PlaceholderScreen
      title="Arrival"
      description="Marking arrival at the pickup point is a ride-lifecycle transition (Phase 9) that doesn't exist until rides do."
    />
  );
}
