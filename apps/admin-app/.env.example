# Copy this file to .env.local and fill in real (development-only) values.
# Never commit .env.local. Never put real secrets here.
# NEXT_PUBLIC_* variables are bundled into client-side JS — never put
# secrets in a NEXT_PUBLIC_ variable.

# Base URL of the backend API this admin app talks to.
NEXT_PUBLIC_API_URL=http://localhost:4000
