{
  "name": "rideshare-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Rideshare platform monorepo (Stage 1 — development/technical validation, not for commercial launch).",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:admin": "npm run dev --workspace=apps/admin-app",
    "dev:passenger": "npm run start --workspace=apps/passenger-app",
    "dev:driver": "npm run start --workspace=apps/driver-app",
    "build:packages": "npm run build --workspace=packages/types && npm run build --workspace=packages/design-tokens && npm run build --workspace=packages/logging && npm run build --workspace=packages/auth && npm run build --workspace=packages/validation && npm run build --workspace=packages/maps && npm run build --workspace=packages/pricing && npm run build --workspace=packages/matching && npm run build --workspace=packages/payments && npm run build --workspace=packages/storage && npm run build --workspace=packages/screening && npm run build --workspace=packages/notifications && npm run build --workspace=packages/observability && npm run build --workspace=packages/database",
    "build": "npm run build:packages && npm run build --workspace=apps/api && npm run build --workspace=apps/admin-app",
    "build:api": "npm run build --workspace=apps/api",
    "build:admin": "npm run build --workspace=apps/admin-app",
    "db:migrate": "npm run migrate --workspace=packages/database",
    "db:seed": "npm run seed --workspace=packages/database",
    "db:reset": "npm run reset --workspace=packages/database",
    "simulate:drivers": "npm run simulate:drivers --workspace=apps/api",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~6.0.3"
  }
}
