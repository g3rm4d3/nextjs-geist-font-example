{
  "name": "@rideshare/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and API contracts used across rideshare platform apps.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@rideshare/config": "*",
    "typescript": "~6.0.3",
    "vitest": "^4.1.10"
  }
}
