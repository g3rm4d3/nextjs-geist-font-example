{
  "name": "@rideshare/screening",
  "version": "0.1.0",
  "private": true,
  "description": "BackgroundCheckProvider abstraction (section 13) — MOCK ONLY in Stage 1; real background checks are explicitly out of scope (see docs/document-management.md).",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "@rideshare/config": "*",
    "typescript": "~6.0.3",
    "vitest": "^4.1.10"
  }
}
