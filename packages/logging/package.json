{
  "name": "@rideshare/logging",
  "version": "0.1.0",
  "private": true,
  "description": "Shared structured logger for backend services. Redacts sensitive fields by default.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3"
  },
  "devDependencies": {
    "@rideshare/config": "*",
    "@types/node": "^26.2.0",
    "typescript": "~6.0.3",
    "vitest": "^4.1.10"
  }
}
